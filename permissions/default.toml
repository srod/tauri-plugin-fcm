[default]
description = "Default permissions for the FCM plugin"
permissions = [
    "allow-get-token",
    "allow-request-permissions",
    "allow-check-permissions",
    "allow-register",
    "allow-delete-token",
]
